class Scene {
  /** @description
   * Represents a single scene.
   * @constructor
  */
  constructor(id) {
    this.id = id;
    this.elements = [];
  }

  /*
    * renders the scene on the given canvas
    * @param {int} c - the canvas to draw the scene on.
  */
  render(c){

  }
}
